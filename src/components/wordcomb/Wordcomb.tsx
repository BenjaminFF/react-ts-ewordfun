import React, { FC, FormEvent } from 'react'


interface Props {
    value?: string
    onChange?: any
    name?: string
}

const Wordcomb: React.FC<Props> = ({ value, onChange, name }) => {

    return (
        <div>
            
        </div>
    )
}

export default Wordcomb

